# CLAUDE.md

### My Workflow Skills (Curated)

IMPORTANT: Do NOT evaluate all skills on every message — only activate skills when the task clearly matches.

**Discipline (activate when relevant):**
- `ce:systematic-debugging` — 4-phase root cause before fix. USE WHEN: any bug, error, unexpected behavior, failed test.
- Verify before done — run the verification commands and show evidence before claiming anything is done/fixed/working. No skill needed; this is expected native behavior.
- `ce:handling-errors` — no silent failures, preserve context. USE WHEN: writing try/catch, designing error propagation.
- `ce:writing-tests` — Testing Trophy (integration > unit). USE WHEN: adding tests, choosing test strategy.

**Operations (invoke explicitly via slash command):**
- `/review` — pre-landing PR diff review (gstack)
- `/ship` — test, review, version, changelog, push, PR (gstack)
- `/qa` — real browser QA, find and fix bugs (gstack)
- `/investigate` — browser-aided root cause debugging (gstack)
- `/browse` — persistent headless browser for anything (gstack)

**Planning (when decomposition is needed):**
- Plan mode — spec/requirements → written implementation plan (native)
- `ce:executing-plans` — dispatch grouped tasks in waves
- `/iterate` — lightweight discovery loop for iterative/exploratory work

**Occasional (activate only when directly relevant):**
- `ce:design` — precise minimal UI design (Jony Ive craft)
- `ce:architecting-systems` — system design and technical docs
- `ce:optimizing-performance` — measure-first optimization
- `ce:visualizing-with-mermaid` — professional diagrams
- `ce:fixing-flaky-tests` — fix flaky async tests (was condition-based-waiting)
- `ce:log-reader` agent — efficient log analysis (filter before read)

**Quick reference:** type `/w` to see this menu with usage hints.
